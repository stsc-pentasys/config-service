server:
  port: 9090
management:
  port: 9091
# TODO 2: Add property spring.cloud.config.server.git.url
endpoints:
  metrics:
    sensitive: false
  health:
    sensitive: false
logging:
  level:
    org:
      springframework:
        cloud:
          config: DEBUG
# TODO 4: Add security configuration
